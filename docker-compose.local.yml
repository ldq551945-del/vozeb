services:
  app:
    image: vozeb:local
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - vozeb-data:/app/web/.data
    restart: unless-stopped

volumes:
  vozeb-data:
