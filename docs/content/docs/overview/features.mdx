---
title: 功能介绍
description: 当前项目已实现的主要功能
---

# 功能介绍

本文档记录 VOZEB `v0.9.6` 当前已经实现的主要功能。VOZEB 基于原创开源画布项目继续开发，并保留原作者致谢与 AGPL-3.0 开源协议说明。

## 画布项目

- 支持创建多个画布项目。
- 支持项目重命名、删除、批量选择和批量删除。
- 支持单个画布项目导出为 JSON，也支持从 JSON 导入画布。
- 画布项目保存在浏览器本地，登录账号负责身份和积分，不会自动把画布上传到服务器。

## 无限画布

- 支持拖动画布、滚轮缩放、缩放滑杆和重置视图。
- 支持小地图定位，可开关小地图。
- 支持点阵、网格线、空白三种背景。
- 支持浅色和深色主题。
- 支持框选、多选、全选、取消选择、删除选中。
- 支持复制粘贴节点和节点之间的连线。
- 支持撤销和重做节点、连线、视口、背景和助手会话变化。
- 支持节点连线，并高亮当前节点相关的上下游节点和连线。
- 支持快捷键帮助，覆盖缩放、框选、全选、复制粘贴、撤销重做、删除、退出选择和拖入图片。

## 节点

目前画布中支持多类节点：

- 图片节点：展示上传图片、生成图片或我的素材图片。
- 文本节点：保存提示词、说明文案、AI 文字回答等文本内容。
- 音频节点：保存音频参考或生成结果。
- 视频节点：保存视频参考或生成结果。
- 生成配置节点：汇总上游文本、图片、视频和音频，统一配置模型、比例、数量后批量生成内容。

节点支持：

- 拖拽移动。
- 四角缩放。
- 图片节点等比缩放或自由比例切换。
- 查看节点基础信息和 JSON。
- 删除、复制、粘贴。
- 通过左右连接点建立上下游关系。

## 图片工作流

- 支持上传图片到新节点。
- 支持拖拽图片文件到画布。
- 支持替换已有图片节点内容。
- 支持下载图片节点。
- 支持把图片节点保存到“我的素材”。
- 支持图片裁剪，并把裁剪结果生成为新的图片节点。
- 支持本地多角度变换，并把结果生成为新的图片节点。
- 支持生成失败后重试。
- 批量生成多张图片时会先展示为图片组节点，支持叠卡预览、展开查看全部结果并设置主图。
- 图片工作台支持按生成记录保存结果状态，切换记录后仍可回到原记录继续查看生成中、成功和失败结果；同一记录内再次生成会追加显示，不会替换旧结果。右侧结果支持勾选、全选和批量删除，生成记录标题也可以重命名。
- 视频创作台支持生成记录查看、记录删除、标题重命名和右侧结果删除，避免切换记录或刷新后丢失已生成结果。
- 图片工作台、视频创作台和画布图片/视频生成支持三层兜底访问：用户端优先使用浏览器本地缓存，本地缓存丢失时回退到 API 远程结果地址，远程地址不可用时再按后台开关使用服务器副本。
- 生成资源服务器副本存放在 `.data/generation-assets`，管理员可以分别控制图片/视频是否允许服务器兜底、是否把新结果下载保存到服务器。

## AI 生成

项目默认使用前台直连：前端使用浏览器本地配置的 Base URL、API Key 和 Model 直接请求 OpenAI 兼容接口，不再通过项目服务端转发 AI 请求。

OpenAI 兼容图像和文本能力继续复用现有接口：

- `/v1/images/generations`：文生图。
- `/v1/images/edits`：图生图/参考图编辑。
- `/v1/responses`：文本问答、带图问答和在线 Agent 工具调用。
- `/v1/models`：读取模型列表；火山方舟 Agent Plan 专属 `/api/plan/v3` 当前未提供 OpenAI `/models` 模型列表接口，需要手动填写模型名。

视频能力支持两类接口：

- OpenAI 风格视频：`POST /v1/videos`、`GET /v1/videos/{id}`、`GET /v1/videos/{id}/content`。
- 火山方舟 Agent Plan / Seedance 2.0：Base URL 使用 `https://ark.cn-beijing.volces.com/api/plan/v3`，创建任务为 `POST /contents/generations/tasks`，查询任务为 `GET /contents/generations/tasks/{id}`，成功结果读取 `content.video_url`。

Base URL 如果已经以 `/v1`、`/api/v3` 或 `/api/plan/v3` 结尾，系统不会再追加 `/v1`。因此 cpa 反代或火山方舟 Agent Plan 可以继续通过现有 Base URL + API Key + Model 方式配置，不需要新增火山生图 Provider。

配置弹窗里的“拉取模型列表”会尝试真实请求 OpenAI `/models`，不会为 Agent Plan 伪造模型结果。如果火山方舟 Agent Plan 返回 404，请手动增加 `doubao-seedance-2.0` 或文档列出的其他模型名。

可配置项：

- Base URL。
- API Key。
- 默认模型。
- 图片质量。
- 图片比例。
- 生成数量。
- 视频模型。
- 视频比例、清晰度、时长、生成声音和水印。

普通图片/文本节点可以直接输入提示词生成结果。生成配置节点可以读取上游节点内容，并按节点自己的配置批量生成多个图片或文本结果。生成配置节点支持预览当前提示词和参考图输入，并调整输入顺序。

图片工作台、画布图片生成、画布视频生成和画布文本/反推提示词会保存任务 ID 并轮询结果。页面刷新或切换记录后可继续查询未完成任务，避免异步请求返回时前端已经拿不到结果。

视频生成可从文本节点读取 prompt，从图片节点读取参考图，从视频节点读取参考视频，从音频节点读取参考音频。Seedance 2.0 支持最多 9 张参考图、3 个参考视频、3 个参考音频；分辨率支持 `480p`、`720p`、`1080p`（fast 模型不支持 `1080p`），比例支持 `16:9`、`4:3`、`1:1`、`3:4`、`9:16`、`21:9`、`adaptive`，时长支持 4-15 秒或智能时长。生成成功后会把视频插入画布为视频节点并使用原生播放器预览。参考视频和参考音频优先使用公网可访问 URL；本地素材会以前端可读取的数据传给兼容接口，是否支持取决于具体上游。

## 画布助手

画布右侧助手面板支持：

- 文本问答。
- 生图。
- 读取当前选中节点作为引用。
- 自动把选中节点的上游节点也纳入引用。
- 粘贴图片到助手输入框并插入画布。
- 历史会话。
- 删除单条或多条会话。
- 重试回答。
- 把助手生成的文本插入画布。
- 把助手生成的图片插入画布。
- 折叠和展开助手面板。

## 提示词库

前台提示词库支持：

- 按标题搜索。
- 按标签筛选。
- 按分类和标签筛选。
- 查看提示词详情。
- 查看封面和结果图。
- 复制提示词。
- 把提示词加入“我的素材”。

管理员提示词管理支持：

- 新增、删除公共提示词。
- 按标题、分类、标签、封面、备注和提示词正文分页搜索，切换后台提示词库时只加载当前页。
- 多选公共提示词并批量删除，适合维护大型提示词库。
- 设置标题、分类、标签、封面 URL、提示词正文和备注。
- 手机端公共提示词列表使用卡片布局，避免表格横向溢出。
- 在后台表格中直接查看封面缩略图，确认图片是否可显示。
- 首次打开公共提示词库时，采集原作者接入过的远程提示词源。

当前采集源包括多个 GPT Image / GPT-4o / Nano Banana Pro 相关提示词仓库。VOZEB 只保留可访问的远程图片 URL 作为 `coverUrl`，不会把这些封面图写入浏览器本地素材存储，也不会转换为 `blob:` 或 `data:` 图片。

## 素材

“我的素材”是浏览器本地素材库，支持：

- 新增文本素材和图片素材。
- 编辑素材标题、封面、标签、来源、备注和内容。
- 删除素材。
- 按关键词搜索。
- 按类型筛选。
- 分页浏览。
- 复制文本素材。
- 下载图片素材。
- 从提示词库和画布节点加入素材。
- 在画布中插入素材。

## 配置和同步

- 当前版本提供账号密码登录、注册开关、每日签到和管理员后台。
- 登录 Cookie 会按 HTTP 直连或 HTTPS 反向代理自动选择安全模式，特殊代理可用 `VOZEB_COOKIE_SECURE` 强制指定。
- 站点首次注册的账号会自动成为管理员，用于完成网站初始化。
- 管理员后台采用侧边栏切换布局，支持概览、系统设置、用户管理和公共提示词库。
- 管理员可配置注册开关、初始积分、签到奖励、系统模型渠道、默认模型和是否允许用户自配接口。
- 管理员可在网站设置中维护首页品牌、Logo、SEO、社交媒体、友情链接和首页提示词展示；首页提示词展示支持随机公共提示词库和后台自定义两种模式。
- 管理员可搜索用户、批量删除用户，并维护用户昵称、邮箱、角色、账号状态、积分余额和密码。
- 管理员可查看所有用户的图片/视频生成日志、提示词、入口来源、模型、状态、耗时、结果预览、API 远程地址和服务器兜底地址，并支持搜索、日期筛选、勾选删除和全选删除。
- 管理员可查看生成资源占用、丢失引用和未引用文件，并清理不再被日志引用的服务器副本。
- 图片/视频服务器兜底和下载保存开关相互独立：兜底控制用户端是否能使用服务器副本，下载保存控制新生成结果是否写入服务器磁盘。
- 配置与用户偏好弹窗支持多个 OpenAI 兼容渠道、默认模型、生成偏好和 WebDAV 同步设置。
- 右上角版本入口支持检查更新，并从 `csyqlz/vozeb` 读取 `VERSION` 和 `CHANGELOG.md`。

## 当前限制

- 画布项目和“我的素材”目前只保存在浏览器本地，不会随账号同步。
- 管理员公共提示词库保存在服务端 `.data/prompts.json`，Docker / Render 长期使用时建议挂载或备份 `.data`。
- 服务器生成资源副本保存在 `.data/generation-assets`，会随 Docker 数据卷保留；关闭下载保存不会删除旧副本，旧副本可在后台资源统计中清理。
- 用户自配 AI API Key 保存在浏览器本地；管理员系统渠道的 API Key 保存在服务端本地数据文件，适合可信部署环境使用。
- Seedance 本地参考视频/音频更建议使用公网可访问 URL；上游是否接受前端传入的本地数据取决于具体兼容接口。
- Seedance 返回远程视频 URL 时，前端会尽量下载为本地 Blob 持久化；如果因 CORS 或网络限制无法下载，会保留远程 URL，后续是否可播放取决于上游 URL 的有效期。
- 画布更适合桌面端使用，移动端触控体验还未系统完善。
